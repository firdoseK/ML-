import numpy as np
array1 = np.array([[1, 2, 3], [3, 4, 5]])
array2 = np.array([[6, 7, 8], [4, 5, 6]])

added_arr = array1+array2
print(added_arr)
subtracted_arr = array2-array1
print(subtracted_arr)
multiplied_arr = array1*array2
print(multiplied_arr)
